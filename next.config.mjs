/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "source.unsplash.com",
      "images.unsplash.com",
      "res.cloudinary.com",
      "images.clerk.dev",
      "www.gravatar.com",
    ],
  },
};

export default nextConfig;
